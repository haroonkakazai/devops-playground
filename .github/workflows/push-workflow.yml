name: Push Workflow

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  push-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello, Haroon!"